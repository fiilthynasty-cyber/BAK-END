def send_notification(channel: str, message: str) -> bool:
    """Send an alert to a channel.

    This placeholder returns success without external integrations.
    """
    _ = (channel, message)
    return True
